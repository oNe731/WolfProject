using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * #인벤토리/ 아이템
- 초기에는 아이템 슬롯이 비어 있음.
- 첫 번째 회복 아이템을 획득하면 자동으로 슬롯 1에 장착.
 */

public class Item : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
